import { BaseFrontEndApiCalls } from 'smart-db';
import { CampaignMemberEntity } from '../Entities/CampaignMember.Entity';

export class CampaignMemberApi extends BaseFrontEndApiCalls {
    protected static _Entity = CampaignMemberEntity;

    // #region api

    // #endregion api
}
