import { BaseFrontEndApiCalls } from 'smart-db';
import { MilestoneEntity } from '../Entities/Milestone.Entity';

export class MilestoneApi extends BaseFrontEndApiCalls {
    protected static _Entity = MilestoneEntity;

    // #region api

    // #endregion api
}
