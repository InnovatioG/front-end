import { BaseFrontEndApiCalls    } from 'smart-db';
import { CampaignStatusEntity } from '../Entities/CampaignStatus.Entity';

export class CampaignStatusApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = CampaignStatusEntity;

    // #region api

    // #endregion api
}
