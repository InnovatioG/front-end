import { BaseFrontEndApiCalls } from 'smart-db';
import { CampaignContentEntity } from '../Entities/CampaignContent.Entity';

export class CampaignContentApi extends BaseFrontEndApiCalls {
    protected static _Entity = CampaignContentEntity;

    // #region api

    // #endregion api
}
