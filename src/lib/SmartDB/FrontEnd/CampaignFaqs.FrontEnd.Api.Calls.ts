import { BaseFrontEndApiCalls } from 'smart-db';
import { CampaignFaqsEntity } from '../Entities/CampaignFaqs.Entity';

export class CampaignFaqsApi extends BaseFrontEndApiCalls {
    protected static _Entity = CampaignFaqsEntity;

    // #region api

    // #endregion api
}
